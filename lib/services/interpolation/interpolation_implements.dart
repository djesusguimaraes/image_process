part of 'interpolation_package.dart';

class _Bilinear {
  static less(DecodeSettings _set) async {
    _set.sendPort.send('');
  }

  static more(DecodeSettings _set) async {
    _set.sendPort.send('');
  }
}

class _Neighbor {
  static less(DecodeSettings _set) async {
    _set.sendPort.send('');
  }

  static more(DecodeSettings _set) async {
    _set.sendPort.send('');
  }
}
